max_trees = 16
root = "${HOME}/worktrees"

# Worktree root directory (relative to repo root or absolute path).
# Worktrees are placed under {root}/.treehouse/. Default: $HOME
# Example: root = "./"
# Align with factory.ts worker isolation at ~/worktrees/<repo>-<slug>/
